Server task:

Server needs to:
keep track of various rooms,
connect users to each other,
manage communication between players,

Protocol:
Open browser- Client logs in to server.
client visits a web page, switches tabs -> notifies the server what the url or checksum of webpage,
server, if finds valid opponent -> notifies client with push notification